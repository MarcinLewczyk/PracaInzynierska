<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    tools:context="lewczyk.pracainzynierska.UserExercise.UserExercisePlanList.UserExercisePlanListActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/userExercisePlanListLinearLayout"
        android:weightSum="0.5">
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:gravity="start|center_vertical"
            android:textSize="17sp"
            android:textColor="@color/colorPrimaryText"
            android:layout_marginLeft="15dp"
            android:text="@string/plans_name"
            android:layout_weight="0.5" />
    </LinearLayout>

    <ListView
        android:layout_below="@+id/userExercisePlanListLinearLayout"
        android:id="@+id/userTrainingPlanListView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
    </ListView>

</RelativeLayout>